<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fit4u">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fit4U"
        tools:targetApi="31">

        <!-- Outfit -->
        <activity android:name=".ui.outfit.SavedOutfitsActivity" />
        <activity android:name=".ui.outfit.OutfitTodayActivity" />

        <!-- Home -->
        <activity android:name=".ui.home.HomeActivity" />
        <activity
            android:name=".ui.home.IntroVideoActivity"
            android:exported="false"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />

        <!-- Closet -->
        <activity android:name=".ui.closet.ClosetActivity" />
        <activity android:name=".ui.closet.ItemDetailActivity" />

        <!-- Picker -->
        <activity android:name=".ui.pick.PickItemActivity" />

        <!-- Auth -->
        <activity android:name=".ui.auth.SignUpActivity" />

        <!-- Launcher -->
        <activity
            android:name=".ui.auth.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>
